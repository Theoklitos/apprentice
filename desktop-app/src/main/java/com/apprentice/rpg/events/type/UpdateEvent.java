package com.apprentice.rpg.events.type;

/**
 * Any event that is caused by the update (chaning of the internal data of) an item
 * 
 * @author theoklitos
 * 
 */
public interface UpdateEvent<T> extends ApprenticeEvent<T> {
	// marker interface
}
