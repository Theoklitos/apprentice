package com.apprentice.rpg.events.type;

/**
 * Any event that is caused by an item's deletion
 * 
 * @author theoklitos
 *
 */
public interface DeletionEvent<T> extends ApprenticeEvent<T> {
	// marker interface
}
